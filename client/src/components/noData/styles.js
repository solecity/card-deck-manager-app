import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    marginTop: "10rem",
    color: "rgba(255, 255, 255, 0.5)"
  },
  icon: {
    marginBottom: "1.5rem",
    fontSize: "4rem"
  }
}));
