import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    flexGrow: 1
  },
  title: {
    flexGrow: 1,
    verticalAlign: "middle",
    display: "inline-flex",
    alignItems: "center"
  },
  icon: {
    marginRight: ".5rem"
  }
}));
