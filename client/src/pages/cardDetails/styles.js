import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    padding: "2rem"
  },
  title: {
    paddingBottom: "1.8rem"
  },
  list: {
    marginTop: "2rem"
  }
}));
