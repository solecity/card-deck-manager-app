import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    padding: "2rem"
  },
  title: {
    paddingBottom: "1.8rem"
  },
  table: {
    marginTop: "4rem"
  }
}));
