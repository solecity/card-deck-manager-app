import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  card: {
    height: 135
  },
  buttons: {
    marginRight: ".2rem",
    justifyContent: "flex-end"
  }
}));
