import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    marginTop: "3rem"
  },
  box: {
    borderRight: "1.8px solid #cfcfcf"
  },
  button: {
    marginTop: theme.spacing(2)
  },
  subtitle: {
    marginLeft: theme.spacing(2)
  }
}));
